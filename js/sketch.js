function setup(){
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(51);
  stroke(255);
  ellipse(width/2, height/2, 330, 330);
  text('I have to learn Git and Github!', width/2, height/2);
  
}